# utils/fast_filter.py

def flatten_doc_blocks(struct):
    """If you have a tree, flatten to a list for embedding/batch processing."""
    # Here, we assume the parsing already gives a flat list!
    return struct
